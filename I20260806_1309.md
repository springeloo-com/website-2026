**No JavaScript function** is in that Desktop Figma file — Figma doesn’t store website JS. What exists is a **prototype hover reaction** on the menu.

### Menu hover (found)

Component: `Assets/Components/Element/menue-point-01` (`739:15189`)

| State | Variant | Underline |
|--------|---------|-----------|
| Default | `Name=Default` (`737:14465`) | width ≈ `0` |
| Hover | `Name=Mouseover` (`739:15200`) | width `77` (full label) |

**Prototype reaction on each menu point:**
- Trigger: `ON_HOVER`
- Action: `CHANGE_TO` → Mouseover variant
- Transition: Smart Animate, Ease Out, **300ms**

Clicks are separate `ON_CLICK` → navigate to subpages (Projektunterstützung, Produkte, Springeloo, Kontakt).

### Not present
- No JS/`mouseenter`/`addEventListener` code
- No dropdown open-on-hover script in the menu
- No `SET_VARIABLE` tied to menu hover (that pattern appears elsewhere, e.g. hero image)

So for the site: implement hover as a **300ms underline grow** (CSS/`mouseenter`), not by fetching JS from Fig
