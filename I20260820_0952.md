## Implementation status

**Branch:** `006-sveltia-cms`  
**Tasks:** 18/24 done — 6 need your GitHub login

### Done
| Change | Detail |
|--------|--------|
| Dependency | `@sveltia/cms` in, `decap-cms` out |
| Build | `scripts/copy-sveltia.mjs` → `public/admin/sveltia-cms.js` |
| Admin shell | `public/admin/index.html` loads Sveltia |
| Clean-up | Removed `copy-decap.mjs`, `check-decap.sh`, Decap bundle |
| Docs | `docs/howto-cms.md` (was howto-decap), README + related howtos updated |
| Build | `npm run build` OK — `dist/admin/` has only `index.html`, `config.yml`, `sveltia-cms.js` |
| Local verify | `/admin/` shows **Sveltia CMS** login (GitHub / Local / Access Token) |

`config.yml` was not changed (as planned). OAuth proxy hostname still contains `decap` — that’s the existing Worker URL, intentional.

### Needs you (manual)
Open `http://localhost:4321/admin/` (or preview), sign in with GitHub, then:

1. **T008–T009** — confirm Globals / Home / Produkte + field parity  
2. **T010–T012** — save a test edit → PR → discard  
3. **T024** — full quickstart pass  

### Next
Say if you want me to **commit** these changes (and optionally open a PR). After merge + deploy, production `/admin/` will serve Sveltia instead of Decap.
