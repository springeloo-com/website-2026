Deploy failed because **GitHub Pages isn’t available on a private Free-plan org repo** — that caused the 404.

### Fixed
1. **Workflow** — runs only on push to `main` (+ manual `workflow_dispatch`); bumped to `checkout@v5`, `setup-node@v5`, `upload-pages-artifact@v5`, `deploy-pages@v5`.
2. **Repo** — set to **public** so Pages works on Free.
3. **Pages** — enabled with source **GitHub Actions**.
4. **Variables** — `PUBLIC_BASE_PATH=/website-2026/`, `PUBLIC_SITE_URL=https://springeloo-com.github.io/website-2026`.

Expected URL after a successful deploy: https://springeloo-com.github.io/website-2026/

The workflow change is still on branch `002-cloudcannon-editing`. Merge/push it to `main` (or cherry-pick the workflow commit) so the next deploy uses the updated pipeline.

If the repo must stay private, you’ll need a paid org plan that includes private Pages — say if you want that reversed and a different hosting approach.