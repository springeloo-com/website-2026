Both links resolve via MCP. One label is wrong relative to the Figma layer names.

| What you called it | Proto node | Actual layer name | Size |
|---|---|---|---|
| Desktop | [`2108:58670`](https://www.figma.com/proto/QLSDfzdupEsnPJ4WY528O5/Springeloo-%7C-Webdesign?node-id=2108-58670&page-id=1969%3A37969) | `01-landingpage-Desktop` | 1440×900 |
| Tablet hoch | [`2108:58675`](https://www.figma.com/proto/QLSDfzdupEsnPJ4WY528O5/Springeloo-%7C-Webdesign?node-id=2108-58675&page-id=1969%3A37969) | `01-landingpage-Tablet-quer` | 1024×768 |

True **Tablet hoch** in the file is `2108:58680` (768×1024). Mobile is `2108:58686` (440×956).

So the second link is landscape tablet, not portrait. The contract in `figma-source.md` currently has hoch/quer swapped the same way.

Also hit the Figma MCP **View-seat rate limit**, so further design-context calls will need a short wait or a seat upgrade.

Want me to fix the node mapping in the contract, then continue the homepage design-to-code pass once the limit resets?

##

Yes — hoch and quer were swapped. Corrected mapping (from Figma layer names):

| Frame | Node | Canvas | Acceptance width |
|-------|------|--------|------------------|
| Desktop | `2108:58670` | 1440×900 | 1280px |
| Tablet quer | `2108:58675` | 1024×768 | 1024px |
| Tablet hoch | `2108:58680` | 768×1024 | 768px |
| Mobile | `2108:58686` | 440×956 | 390px |

Updated in `figma-source.md`, `visual-qa.md`, `spec.md` FR-012, `data-model.md`, and the homepage QA checklist. Breakpoint **widths** stay the same; only the node labels were wrong.

##

Still failing after re-auth. Upgrade is live, but on the wrong plan for this file.

### What we see
| Check | Result |
|-------|--------|
| `whoami` | **Full** seat, **pro** tier on “Thomas Kamsker's team” |
| `get_design_context` on `QLSDfzdupEsnPJ4WY528O5` | Still: **View seat on Professional** — monthly MCP quota used |

Your personal team is upgraded. The Springeloo file is almost certainly on **another** Professional workspace where you only have **View** — and that seat’s **6 calls/month** is what’s blocking us.

### What to do so we can proceed
1. Open the [Springeloo \| Webdesign](https://www.figma.com/design/QLSDfzdupEsnPJ4WY528O5) file in Figma.
2. Check which **team/org** owns it (not “Thomas Kamsker's team”).
3. On **that** team, upgrade your seat to **Dev** or **Full**  
   ([upgrade link](https://www.figma.com/files/1327625180699439851/recents-and-sharing?upgrade_request_type=expert&entry_point=mcp_rate_limit_deeplink)).
4. Or move/duplicate the file into “Thomas Kamsker's team” (where you already have Pro Full).

Then say **retry** and we’ll fetch again. Until the seat on the file’s owning team is Dev/Full, MCP reads will stay blocked.