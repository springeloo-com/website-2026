Kontakt desktop is implemented from Figma `2109:78611`.

### What changed
- Dark ink page with cream type, grid, intro + side kicker  
- Bordered contact box using Decap `contact.phone` / `contact.email`  
- Office block from globals  
- Management 3-up (portraits, tags, LinkedIn)  
- Cream `SiteFooter` unchanged  
- Team assets under `public/uploads/team/`

### Preview
```bash
npm run build && npm run preview
```
Open `/kontakt` and hard-refresh.

**Note:** Mathias’ Figma export is only ~3 KB (likely a weak asset). Franz/Horst look fine — swap Mathias when you have a better photo.

Want Produkte or Projektunterstützung next?