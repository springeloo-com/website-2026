**Cause:** The public site was fine. Admin stayed blank because **unpkg** failed to load the Decap script on GitHub Pages (`responseStatus: 0`). Switching to **jsDelivr** shows the login UI (“Mit GitHub einloggen”).

**Fix pushed:** `b79eb43` — admin now loads Decap from jsDelivr.

### After Pages deploys (~1–2 min)

1. Hard-refresh: https://springeloo-com.github.io/website-2026/admin/
2. You should see the Decap login button (not a black page)

### If login still fails after the UI appears

Update the Cloudflare Worker `/auth` handler to pass `redirect_uri` (updated in `docs/howto-claudflare.md`), then redeploy the Worker. Confirm the GitHub OAuth App has:

- Homepage: `https://springeloo-decap-oauth.mf-7e0.workers.dev`
- Callback: `https://springeloo-decap-oauth.mf-7e0.workers.dev/callback`
- Secrets: `GITHUB_OAUTH_ID` / `GITHUB_OAUTH_SECRET` on the Worker
- Your GitHub user has **write** on `springeloo-com/website-2026`

Tell me whether you still get a blank page or the login button with an OAuth error.


## 

**What your HTML dump means**

That dump is mostly **expected**:

1. Empty `<body>` in View Source is normal — Decap injects `#nc-root` **after** JS runs  
2. Toastify CSS in `<head>` means the script **started**  
3. You should see **Mit GitHub einloggen** on the page (not in View Source)

CDN loading was unreliable (unpkg often fails with status 0). That’s now fixed.

### Fix pushed (`d7b74e8`)

Decap is **self-hosted** from the build:

- `npm` dependency `decap-cms-app@3.8.0`
- `prebuild` copies it to `public/admin/decap-cms.js`
- Admin loads `./decap-cms.js` (no CDN)

### Check locally after deploy

```bash
npm run check:decap
```

Or:

```bash
curl -sS "https://springeloo-com.github.io/website-2026/admin/" | head -20
curl -sSI "https://springeloo-com.github.io/website-2026/admin/decap-cms.js"
curl -sS "https://springeloo-decap-oauth.mf-7e0.workers.dev/"
```

Expect `decap-cms.js` → **HTTP 200** (~4.5 MB).

Then open https://springeloo-com.github.io/website-2026/admin/  
Hard refresh (Cmd+Shift+R). You should see the Decap login button.

If the button appears but GitHub login fails → next step is the OAuth Worker `redirect_uri` / secrets (not the blank page).