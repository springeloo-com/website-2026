OAuth is correct now (`gho_` + `repo,user`). The only remaining block is **org third-party access**.

**Org owner** must grant the new OAuth App (`Ov23li1E4U4woL6vLNg3`):

1. Open  
   https://github.com/organizations/springeloo-com/settings/oauth_application_policy  
2. Find the Decap OAuth App → **Grant** for `springeloo-com`

Or: **Settings → Applications → Authorized OAuth Apps** → open the app → **Organization access** → **Grant** for `springeloo-com`.

Then save again in Decap (re-login only if Grant still fails). Re-run:

```bash
export GH_TOKEN='gho_...'   # after Grant; do not paste tokens into chat
bash scripts/check-oauth-chain.sh
```

Expect create-ref **HTTP 201**.

Also: that `gho_…` token appeared in the script output (and in chat) — revoke/re-login when convenient. The script no longer prints the token value.