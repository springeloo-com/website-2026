| Checklist | Total | Completed | Incomplete | Status |
|-----------|-------|-----------|------------|--------|
| requirements.md | 16 | 16 | 0 | ✓ PASS |

**Implementation complete** — all **26 tasks** marked done; `npm run build` succeeds.

### What shipped
- **Decap admin**: `public/admin/index.html` + `public/admin/config.yml` (GitHub backend, `editorial_workflow`, media → `public/uploads/`)
- **Collections**: globals + home (text, Markdown bodies, hero/optional card images); **href**/ids/`linkedIn` as **hidden** widgets so saves don’t wipe them; Leistungen fixed at **3**
- **CloudCannon removed**: deleted `cloudcannon.config.yml`; how-to retargeted
- **Docs**: `docs/howto-decap.md` (OAuth proxy runbook + roles); updated edit/deploy/README
- **Guards**: loaders unchanged; build still fails if cards ≠ 3; Decap JS only on `/admin/`

### Still for the team
1. Deploy a **GitHub OAuth proxy** and replace `backend.base_url` in `public/admin/config.yml`
2. Protect `main` (PR required; editors cannot merge)
3. Walk `specs/003-decap-cms-migration/quickstart.md` after OAuth works

Admin (project site): `https://springeloo-com.github.io/website-2026/admin/`

Say if you want a commit on `003-decap-cms-migration`.