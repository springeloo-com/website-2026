**Verdict:** Keep Astro + Decap + GitHub Pages. Use **Figma MCP + Cursor** for layout updates, plus a **tokens refresh** first. Continuous auto-1:1 from Figma is not realistic; a repeatable MCP pass with breakpoint QA is.

Full options + SWOT are in the canvas: `figma-to-static-options.canvas.tsx` (open beside chat).

### Current fit
You already have Figma-derived `tokens.css` / `breakpoints.css` (390 / 768 / 1024 / 1280), Astro components, and Decap for home/globals copy only. Layout is code-owned; media queries are inconsistent (ad-hoc 900/960). That matches Perplexity’s “MCP into the repo” path, not a plugin dump or Framer.

### Options
| ID | Approach | Fit here |
|----|----------|----------|
| **A** | Figma MCP + Cursor → edit Astro/CSS | **Best** — keeps Pages + Decap |
| **B** | Anima / figma.to.website / figma2html | Fast dump, weak for your stack |
| **C** | Hand-code, MCP as reference | Highest craft, slower |
| **D** | Framer etc. | Drops GH Pages + Decap — reject |
| **E** | Variables → CSS tokens only | Always do as step 0; not full 1:1 |

### How to proceed
1. **Prep Figma** — named frames Desktop / Tablet quer / Tablet hoch / Mobile + Auto Layout  
2. **Tokens** — sync Variables → `tokens.css` / `breakpoints.css`; unify `@media` to `--bp-*`  
3. **Home 1:1** via MCP per frame → visual QA at all four widths  
4. **Subpages** same way  
5. **CMS boundary** stays: copy/images in Decap, layout in `.astro`  
6. **Cadence** on each design drop: token diff → MCP pass → QA → merge  

Next concrete step: share the Figma file link (or select frames in Figma Desktop MCP) and we start phase 0–1 against the home page.