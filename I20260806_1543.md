The punycode warning is harmless. The real failure is what comes after: GitHub creates the Pages deployment, then it never leaves deployment_queued, and the action times out.

What’s going on

The hover fix is already built and on the gh-pages branch (nav-link__underline is there).
Live site is still from 5 Aug (old expandable nav).
Every publish path fails at the same step — GitHub’s Pages CDN never picks up the deployment. Status shows operational, but this repo’s queue is wedged.
What we changed

Switched deploy to publish into gh-pages (build succeeds: run).
Legacy “pages build and deployment” is still stuck on Deploy: run.
Next options

Wait / open a GitHub Support ticket for Pages stuck at deployment_queued on springeloo-com/website-2026.
Publish elsewhere for now (Cloudflare Pages / Netlify) from gh-pages or dist.
Retry later — sometimes the queue clears on its own.
I can wire up an alternate host if you want that path.

Those earlier redeploy attempts all hit the same wall: builds succeeded, but GitHub Pages never left deployment_queued.

Current status is unchanged — live site is still 5 Aug (old expandable nav). The hover fix is on gh-pages; the CDN publish step is still stuck.