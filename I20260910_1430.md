Done on `feature/google-tag-manager`. Build passes.

**What shipped**
- Consent Mode v2 defaults in `BaseLayout` (everything denied until choice)
- Cookie banner with **Nur notwendige** / **Analytics erlauben** / **Alle akzeptieren**
- GTM loads only after analytics/all consent (`PUBLIC_GTM_ID`)
- Choice stored in `localStorage` (`springeloo-consent`)
- Footer **Cookie-Einstellungen** reopens the banner
- Datenschutz text on Kontakt updated; deploy workflow + `.env.example` document `PUBLIC_GTM_ID`

**To go live**
1. Create a GTM container and set GitHub Actions variable `PUBLIC_GTM_ID` to `GTM-…`
2. Optionally copy `.env.example` → `.env` for local testing
3. Say if you want a commit / PR
